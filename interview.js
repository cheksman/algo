// Write a function that takes an array of arbitrary intervals, merges any overlapping intervals, and returns the new intervals in no particular order.
// [[1, 2], [3, 5], [4, 7], [6, 8], [9, 10]]
// [[1, 2], [3, 8], [9, 10]]

function overlappingInterval(array) {
    let arr1 = []

    for (let i = 0; i < array.length; i++) {
        arr1 = array[i];
    }
}

